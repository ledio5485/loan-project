server:
  port: 0

spring:
  datasource:
    url: "jdbc:postgresql://localhost:5432/customer"
    username: "postgres"
    password: "password"
    driver-class-name: "org.postgresql.Driver"

eureka:
  client:
    enabled: false
